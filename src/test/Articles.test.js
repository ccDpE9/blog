const assert = require("assert");

import Articles from "../components/Articles";

describe("Articles component", () => {

  // --- Hooks
  before(() => {
    // 1. analize the output of dynamodb call
  });

  afterEach(() => {
  });

  it("Should return text if no articles found", () => {
  });

  it("Should display only 10 articles per page", () => {
    // test page 1
    // test page 3
    // test page 9
  });

  it("Should display articles title, description and timestamp", () => {
  });

  it("Title element is a link to a particular article", () => {
  });

  describe("fetch()", () => {
    it("If no error, should return articles", () => {
    });
  });


});
