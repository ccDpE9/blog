import App from '../../App';
import Article from '../Article';

beforeEach(() => {
});

it('shows an article title', () => {
});

it('shows an article description', () => {
});

it('shows an article timestamps', () => {
});

it('shows an article body', () => {
});
